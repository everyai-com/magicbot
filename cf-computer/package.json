{
  "name": "magicbot-cf-computer",
  "private": true,
  "version": "0.1.0",
  "description": "Cloudflare Worker + Sandbox container that gives each MagicBot bot a cloud computer (the hosted replacement for box.ascii.dev).",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@cloudflare/sandbox": "^0.7.0"
  },
  "devDependencies": {
    "typescript": "^5.9.0",
    "wrangler": "^4.0.0"
  }
}
