{
  "name": "magicbot-cf-computer",
  "private": true,
  "version": "0.1.0",
  "description": "Official @cloudflare/computer workspace and Linux runtime for every MagicBot bot.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@cloudflare/computer": "0.2.1",
    "@cloudflare/sandbox": "^0.7.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260812.1",
    "typescript": "^5.9.0",
    "wrangler": "^4.0.0"
  }
}
